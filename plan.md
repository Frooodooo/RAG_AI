1. **Add `aria-label` to `RenameInput` using `replace_with_git_merge_diff`**
   - Use `replace_with_git_merge_diff` to add `aria-label="Rename conversation"` to the `<input>` element returned by `RenameInput` in `rag-demo/src/components/Chat/SessionSidebar.tsx`.
2. **Verify Changes via Linting and Tests**
   - Run `cd rag-demo && pnpm lint` and `cd rag-demo && pnpm test` to ensure there are no breaking changes.
3. **Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.**
4. **Submit the PR**
